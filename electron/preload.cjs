// Preload runs in an isolated context before the renderer loads. Empty for now —
// add contextBridge IPC APIs here if the renderer ever needs native access.
window.addEventListener('DOMContentLoaded', () => {});
